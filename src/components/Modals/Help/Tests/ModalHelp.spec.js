/* eslint-disable no-undef */
import { mount } from '@vue/test-utils';
import { h } from 'vue';
import ModalHelp from '../ModalHelp.vue';

// describe('InputPassword.vue', async () => {
//     it('should render ModalHelp', () => {
//         const wrapper = mount(ModalHelp, {
//             provide: () => ({
//                 i18ntc: 'i18ntc'        // to prevent warnings because there was no provider in the testset
//             }),
//         });
//         await wrapper.setProps({ visible: true });
//
//         //dialog v-model:visible="display"
//         expect(wrapper.html()).toContain('p-dialog');
//     });
// });

describe('Button.vue', () => {
    test.skip('ddd', () => {});
    // it('should render ModalHelp', async () => {
    //     const wrapper = mount(ModalHelp, {
    //         props: {
    //             visible: true,
    //         },
    //     });
    //
    //     await wrapper.setProps({ visible: true });
    //
    //     expect(wrapper.html()).toContain('p-dialog');
    // });
});
